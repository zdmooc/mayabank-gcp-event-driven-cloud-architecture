# Networking module — planned

The Cloud Run MVP does not introduce a custom VPC merely to demonstrate networking. Private Cloud SQL connectivity, VPC egress, PSC/VPC-SC or enterprise hub/spoke topology will be implemented only when the security/runtime requirement justifies them.

Status: `DESIGNED / NOT_IMPLEMENTED`.
